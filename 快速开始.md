# 快速开始指南 - Windows 本地运行

## ⚡ 超快速启动（3步）

### 1️⃣ 安装依赖（首次运行）

双击运行 `install.bat`

或在命令行执行：
```bash
pip install -r app\requirements.txt
pip install python-dotenv
```

### 2️⃣ 启动服务

**方式A：双击运行 `run.bat`** ✅ 推荐

**方式B：命令行运行**
```bash
python start.py
```

### 3️⃣ 验证服务

浏览器访问：http://localhost:8050

看到以下内容表示成功：
```json
{
  "status": "ok",
  "message": "OpenAI to Gemini Adapter is running."
}
```

---

## 📝 配置信息

已配置的凭证（在 `.env` 文件中）：
- **API密钥**: 请查看 `.env` 文件
- **Vertex Express Key**: 已配置
- **GCP项目ID**: `1053533920089`
- **GCP区域**: `us-central1`

---

## 🧪 测试 API

### 查看可用模型

```bash
curl http://localhost:8050/v1/models -H "Authorization: Bearer your_secure_password"
```

### 发送聊天请求

**PowerShell:**
```powershell
curl -X POST http://localhost:8050/v1/chat/completions `
  -H "Content-Type: application/json" `
  -H "Authorization: Bearer your_secure_password" `
  -d '{"model": "gemini-1.5-flash-latest", "messages": [{"role": "user", "content": "你好，介绍一下自己"}]}'
```

**CMD:**
```cmd
curl -X POST http://localhost:8050/v1/chat/completions ^
  -H "Content-Type: application/json" ^
  -H "Authorization: Bearer your_secure_password" ^
  -d "{\"model\": \"gemini-1.5-flash-latest\", \"messages\": [{\"role\": \"user\", \"content\": \"你好，介绍一下自己\"}]}"
```

---

## 📚 在应用中使用

这个服务提供 OpenAI 兼容的 API，可以在任何支持 OpenAI API 的应用中使用：

**配置示例：**
- **API地址**: `http://localhost:8050/v1`
- **API密钥**: `your_secure_password` (请查看 .env)
- **模型**: `gemini-1.5-flash-latest` 或 `gemini-1.5-pro-latest`

---

## ❓ 常见问题

**Q: 端口被占用怎么办？**

编辑 `start.py`，修改端口号：
```python
uvicorn.run(
    "app.main:app",
    host="0.0.0.0",
    port=8051,  # 改成其他端口
    reload=True
)
```

**Q: 如何停止服务？**

在运行窗口按 `Ctrl + C`

**Q: 想查看详细日志？**

服务启动时会自动显示详细日志

---

## 🎯 下一步

- 查看 `WINDOWS_SETUP.md` 了解更多配置选项
- 查看 `README.md` 了解完整功能
- 在你喜欢的 AI 应用中配置此 API

---

**提示**: 如果修改了 `.env` 配置，需要重启服务才能生效