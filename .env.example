# Authentication
# Port to expose on the host
APP_PORT=8050
API_KEY=your_secure_password

# HuggingFace Settings
HUGGINGFACE=false
HUGGINGFACE_API_KEY=

# Vertex AI Credentials
# Directory for service account credential files
CREDENTIALS_DIR=/app/credentials
# JSON string for service account credentials (optional if using files in CREDENTIALS_DIR)
GOOGLE_CREDENTIALS_JSON=
# API Key for Vertex Express Mode (comma-separated for multiple keys)
VERTEX_EXPRESS_API_KEY=

# GCP Configuration (Optional but recommended for Express Mode)
GCP_PROJECT_ID=
GCP_LOCATION=us-central1

# Streaming Settings
FAKE_STREAMING=false
FAKE_STREAMING_INTERVAL=1.0

# Model Configuration
# 模型列表优先从本地 vertexModels.json 文件加载，如果需要从远程获取可配置此 URL（留空则只使用本地文件）
MODELS_CONFIG_URL=

# Load Balancing
ROUNDROBIN=false

# Safety Settings
SAFETY_SCORE=false

# Proxy Settings
PROXY_URL=
SSL_CERT_FILE=