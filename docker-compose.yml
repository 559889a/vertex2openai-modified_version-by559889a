version: '3.8'

services:
  vertex2openai:
    build: .
    container_name: vertex2openai
    ports:
      # Map host port to container port 8050
      - "${APP_PORT:-8050}:8050"
    volumes:
      - ./credentials:/app/credentials
    env_file:
      - .env
    restart: unless-stopped
